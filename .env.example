# GCBA API credentials (obtain from https://data.buenosaires.gob.ar)
BA_CLIENT_ID=your_client_id
BA_CLIENT_SECRET=your_client_secret

# Transport mode: "stdio" (default) or "http"
TRANSPORT=stdio

# HTTP server port (only used when TRANSPORT=http)
PORT=3000
